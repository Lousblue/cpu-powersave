# cpu-powersave
Interactive CPU power limiting for Linux. Detects your hardware, asks what to cap - turbo boost, per-core frequency bounds, energy preference, RAPL power limit - then applies it at every boot via systemd. Flags and profiles for unattended use. Intel (intel_pstate) and AMD (amd_pstate).
